# CLAUDE.md bridge for mq5-macos-backtest

When asked to compile `.mq5`, create `.ex5`, run MT5 Strategy Tester, compare MT5 presets, use real ticks, parse MT5 HTML reports, or backtest Expert Advisors on macOS/Wine, follow the workflow in:

```text
./mq5-macos-backtest/SKILL.md
```

If Claude cannot import that file directly, paste the contents of `mq5-macos-backtest/SKILL.md` below this bridge in the target project's `CLAUDE.md`.

